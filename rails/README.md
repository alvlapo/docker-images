Docker base image for rails development and test
================================================

## Pre-install gems
See in Gemfile

## Build image
To build base image use:
```shell
sudo make build
```
